# jlu-drcom-gtk
JLU Drcom Gtk - Client to login to Jilin University network

## Manual work
Please change the mac address in drcom.c before compiling

## How to compile
gcc jdg-main.c drcom.c md5.c -o jlu-drcom-gtk `pkg-config --cflags --libs gtk+-3.0 glib-2.0`

## TODO
* Automatically read mac address
* Connect UI and drcom program
* Add logout functionality
* Login after UI change has been completed
* Use GtkTextEntry instead of fprintf to print debug message
